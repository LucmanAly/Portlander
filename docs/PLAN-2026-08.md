# Portlander — implementation plan review (2026-08-01)

Assessment of the external implementation plan, checked against the live project
(repo at `a01a950`, Supabase `vvstmdnnpjnfvueoecwl`, deployed Edge Functions).

**Verdict: worth doing, with one correction and one reordering.** Workstreams 1–3
fix real, verified defects — two of which can destroy live data. Workstream 4 is
polish and can slip. Step 0 as written is already complete and should be replaced
with a much smaller task.

---

## What was verified against production

Everything checked below was measured, not assumed.

| Claim | Verdict | Evidence |
|-------|---------|----------|
| Edge Functions have drifted from git | ❌ **Stale** | All four deployed sources match the repo. See "Step 0 correction" |
| 9 fabricated `macro-seed` rows in production | ✅ Confirmed | `select source, count(*) from events` → `finnhub` 31, `macro-seed` 9 |
| Score distribution is compressed | ✅ Confirmed exactly | 41 holdings, 1 missing `last_price`, max 13.50%, p90 4.49%, mean 2.44%, median 1.64% |
| Sold positions never disappear | ✅ Confirmed | `snaptrade-sync/index.ts:20` documents it as "Known v1 gap"; upsert-only, no reconciliation |
| CSV import wipes SnapTrade rows | ✅ Confirmed | `PortfolioPage.tsx:66` → `replaceHoldings` → `persistHoldingsRemote` deletes every remote row not in the CSV |
| Signed-out cache leaks the real portfolio | ✅ Confirmed | `storage.ts:5-12` keys are static; `clearAllData()` at `storage.ts:95` has zero call sites |
| One global `lastSyncAt` for three clocks | ✅ Confirmed | `portfolioRepository.ts:76-81` reads the newest `sync_runs` row with no `provider` filter |
| Calendar ignores position weight | ✅ Confirmed | `CalendarPage.tsx:47` passes raw `PortfolioEvent[]`; `MonthCalendar` `dayTone`/`dotColor` branch on type only |
| `EventCard` bar re-encodes the score | ✅ Confirmed | Score at `EventCard.tsx:68`, progress bar at `:84-94` |
| Dividends filter is permanently empty | ✅ Confirmed | No ex-div fetch exists in `sync-events`, `refresh-quotes`, or `snaptrade-sync` |
| Local sync path is dead | ✅ Confirmed | `public/data/` is empty; cloud cron is live |
| `getConfiguredFlag()` is dead | ✅ Confirmed | Defined at `portfolioRepository.ts:291`, never called |
| Weight bar has undocumented ×3 saturation | ✅ Confirmed | `PortfolioTable.tsx:235` |

### Step 0 correction

The plan calls "reconcile deployed Edge Functions into git" blocking, on the
premise that the repo still holds Commercial-only code calling
`registerSnapTradeUser`. **That premise predates PR #13** (merged as `a01a950`).

Current state, verified by fetching each deployed source:

| Function | Deployed | Repo | Match |
|----------|----------|------|-------|
| `snaptrade-connect` | v17 | `SNAPTRADE_AUTH_MODE`, `resolveOwnerUserId`, personal default | ✅ |
| `snaptrade-sync` | v17 | `SnaptradeReader`, `personalReader`/`commercialReader`, owner gate | ✅ |
| `sync-events` | v13 | identical, incl. `buildMacroEvents` and `deterministicUuid` | ✅ |
| `refresh-quotes` | v12 | identical, incl. `day_change_value`/`day_change_pct` | ✅ |

The version numbers (v17/v13/v12) are deploy counters, not drift indicators — they
count every redeploy including identical ones. There is nothing to pull back.

**What survives from Step 0**, and is still worth ~20 minutes:

- Add the anti-drift rule to `AGENTS.md`: never `supabase functions deploy` without
  diffing `get_edge_function` output first. The hazard was real even if this instance
  of it resolved itself.
- Document `SNAPTRADE_AUTH_MODE` / `SNAPTRADE_OWNER_USER_ID` in the two SnapTrade
  function READMEs and `.env.example`.

---

## Revised plan

### P0 — Stop the data loss (half a day)

Two one-click paths currently destroy live data. Neither needs the larger refactor
to be made safe, and neither should wait on it.

**P0.1 — Guard CSV import.** `onCsv` calls `replaceHoldings(parsed)`, which
propagates a remote delete of every row not in the CSV — all 40 SnapTrade rows
included. Minimum viable fix: filter `source === 'snaptrade'` rows out of the
deletion set and preserve them in the replacement list. The full Merge/Replace
picker with a preview (plan 2.2) can follow.

**P0.2 — Clear the cache on sign-out.** Call the existing `clearAllData()` from
`signOut()`, and namespace the six `KEYS` in `storage.ts` by user id. Small, and it
closes a case where a signed-out browser still renders the real book.

### P1 — Data truthfulness (1–2 days) → plan Workstream 1

Do this before anything that consumes weights.

**P1.1 — Real macro dates.** Replace `buildMacroEvents()` in `sync-events` with a
static table in `supabase/functions/_shared/macro-calendar.ts`, then delete the 9
`macro-seed` rows and drop the generator in `src/data/macro.ts`.

*Deviation from the plan:* it asks for confirmed dates "through 2027". FOMC dates
are published roughly a year out and BLS release schedules about the same. Table
only what is officially published, mark those `status: 'confirmed'`, and emit
nothing beyond the published horizon — an empty calendar is honest, an extrapolated
one is the bug being fixed.

**P1.2 — Reconcile sold positions.** Build a `seenTickers` set per `snaptrade-sync`
run; delete `holdings` where `user_id = caller AND source = 'snaptrade' AND ticker
NOT IN seenTickers`. Skip the delete entirely if the run returned zero positions or
hit any per-account error. This is data integrity, not cleanup: a ghost position
inflates the denominator in `portfolioTotalValue()` and therefore silently skews
every other holding's weight and score.

**P1.3 — Three freshness clocks.** Read the newest `sync_runs` row *per provider*
(`finnhub`, `finnhub-quotes`, `snaptrade-positions`) and surface three labelled
timestamps.

### P2 — Tests, then mutation safety (2–3 days) → plan Workstreams 5 + 2, swapped

**This is the one ordering change.** The plan schedules tests last, but P3 below is
a rewrite of the exact code path that can wipe the book. Land vitest and the write-path
tests *before* the rewrite, not after it.

- Add vitest + a GitHub Action running `build → lint → test`.
- Cover first: source preservation across manual/CSV/SnapTrade, the P1.2
  reconciliation guard, and quote-refresh field patching.
- Then do plan 2.1 — replace `persistHoldingsRemote`'s select-all → delete-missing →
  upsert-all with per-row writes under the plan's authority model. The client stops
  sending `last_price` / `day_change_*` / `created_at` entirely.
- Then the full 2.2 Merge/Replace picker with preview counts, and a per-row delete
  confirmation.

Remaining coverage (exposure windows, date boundaries, event dedup, the earnings-
rescheduling characterization test) can land alongside P3.

### P3 — Make the score communicate (1–2 days) → plan Workstream 3

Depends on P1 for clean weights.

- **3.1** Mark rows falling back to `costBasis` as estimated rather than mixing cost
  and market value into one total; route `weightOverridePct` through
  `portfolioTotalValue()` so mixed books sum to 100%.
- **3.2** Replace `min(1, weight / 20)` with a portfolio-relative anchor. On today's
  book `max(5, p90 * 1.5)` ≈ 6.7%, turning a 39/49/78 spread into roughly 50/77/99.

  *Trade-off the plan doesn't name:* a portfolio-relative anchor makes scores
  non-comparable over time — a holding's score moves when *other* holdings move. That
  is acceptable because the score's job is ordering, but it is a real argument for
  **3.3** (High/Medium/Low labels): the words stay honest across rebalances in a way
  a bare number does not. Adopt 3.2 and 3.3 together, not separately.
- **3.4** Invert the card hierarchy — weight becomes the hero, score demotes to a tier
  badge, and delete the `EventCard` progress bar that duplicates the number above it.

### P4 — The morning read (optional, slice it) → plan Workstream 4

Genuine improvements, no correctness risk, safe to defer or take piecemeal. Highest
value first:

1. **4.1** Pass scored events to `MonthCalendar` and drive dot size from weight — the
   screen `AGENTS.md` calls the "position-weighted calendar" currently encodes no weight.
2. **4.6** Portfolio search / sort / source filter. At 41 holdings this is a real gap.
3. **4.5** Mobile: compact holding cards instead of the 640px-min scrolling table.
4. **4.2 / 4.3** Today lede and time grouping.
5. **4.7 / 4.8** Smaller fixes and the Settings IA rewrite.

### P5 — Deletions (an afternoon, any time)

Agreed with the plan, all verified dead or permanently empty:

- Dividends filter chip — no ex-div data is fetched in any sync path.
- Local sync path: `scripts/sync-events.mjs`, `src/lib/eventSync.ts`, the boot-time
  `applyLocalEventsSync()` calls (`PortfolioContext.tsx:123,245`), `public/data/`, the
  `sync:events` npm script, and the Settings section documenting it.
- `markSynced` / "Mark synced now" (`SettingsPage.tsx:133`) — writes a timestamp that
  reflects nothing.
- `getConfiguredFlag()`; the `src/hooks/` row in the `AGENTS.md` repo map (no such
  directory).

Keep the commercial SnapTrade branch. It works, it costs nothing at runtime, and
deleting deployed working code is churn — just document it as unsupported.

### Dropped, as the plan proposes

Multi-tenancy hardening (`sync_runs` missing `user_id`, `using (true)` RLS, global
`events` enumeration) is dead scope under personal-use-only. Also not now: AI
analysis, visual redesign, state framework, DB/hosting migration, tax lots, Phase 2
journals/alerts/prep cards.

---

## Execution — PR sequence

Rationale: stop destruction → shrink the surface → build the net → fix the inputs →
fix the writer → fix the output → make it pleasant. The plan's own ordering, with the
tests moved ahead of the refactor they are meant to protect.

### PR 1 · Safety patch ✅ *(this branch)*

- `portfolioRepository.ts` — select `source`, exclude `source='snaptrade'` from
  `deleteIds`. Fixed at the repository layer so no caller can bypass it.
- `PortfolioPage.tsx` — `onCsv` preserves synced rows instead of `replaceHoldings(parsed)`.
- `storage.ts` — `setStorageNamespace(userId)`; keys become
  `portlander.u.<id>.*` when signed in, unprefixed when not. `ensureSeeded()` refuses
  to seed demo rows into a user namespace.
- `PortfolioContext.tsx` — set the namespace on every auth transition; `signOut()`
  calls `clearAllData()` while the user namespace is still addressed.
- `resetDemo()` refuses while signed in, and the Settings button is disabled with a
  reason. Found while implementing: the confirmation promises "remote Supabase rows
  are not deleted", which held at reset time but not afterwards — demo rows landed in
  state with `user` still set, so the next holdings edit persisted them over the real
  book. Same one-click data-loss shape as the CSV bug, so it was fixed here.

### PR 2 · Delete dead weight ✅ *(this branch)*

Before tests, so no test is written against doomed code. **471 lines removed, 35 added.**

- Local sync path: `scripts/sync-events.mjs`, `scripts/tickers.txt`, `src/lib/eventSync.ts`,
  `public/data/`, the `sync:events` npm script, both `applyLocalEventsSync()` calls, and
  the Settings section documenting it.
- `markSynced` (context + Settings button + `markLocalSynced`), `getConfiguredFlag()`,
  the Dividends chip (`isDividendType` and the `dividends` filter branch stay, so the
  chip returns the day ex-div fetching exists).
- Stale docs corrected rather than left: the Settings "Deferred / later" section and the
  `sync-events` README both described cloud mode and the Edge cron as future work when
  both are live. README data-modes table and `.env.example` Finnhub comment likewise.
- `AGENTS.md`: Edge-Function anti-drift rule added; `src/hooks/` repo-map row (no such
  directory) replaced with the real `context/`.

**Already done, no work needed:** `SNAPTRADE_AUTH_MODE` / `SNAPTRADE_OWNER_USER_ID` are
documented in both SnapTrade READMEs and `.env.example` — PR #13 covered it.

### PR 3 · Test harness + CI ✅ *(this branch)*

vitest + jsdom, `npm test`, and `.github/workflows/ci.yml` running build → lint → test
on PRs and pushes to `main`/`develop`. **60 tests across 4 files.**

- `csv.test.ts` — parsing, and `planCsvImport`, extracted from `PortfolioPage` so the
  rule that protects live positions is testable on its own rather than trapped in a
  component.
- `storage.test.ts` — namespace isolation, the sign-out wipe, and the demo-seeding
  refusal from PR 1.
- `portfolioRepository.test.ts` — characterization of `persistHoldingsRemote` against a
  fake Supabase client, so PR 5's rewrite is provably behaviour-preserving.
- `scoring.test.ts` — weights, score components, filters, window boundaries, exposure.

**Six tests are marked `KNOWN DEFECT, pinned` rather than passing quietly.** They lock in
behaviour that is wrong on purpose, each naming the PR that will change it: the
cost-basis fallback with no estimated marker and the mixed book summing to 140% (PR 6 /
3.1); the `/20` clamp that scores a 25% and a 50% position *identically* and lets only
one of 41 holdings reach the red tier (PR 6 / 3.2); the client sending `last_price` and
`day_change_*` it has no authority over (PR 5).

The distribution fixture reproduces the live book's *shape* — 41 positions, max 13.5%,
median 1.64% — with synthetic tickers and values. It deliberately carries no real
holdings.

### PR 4 · Data truthfulness

- `supabase/functions/_shared/macro-calendar.ts`, published dates only; `sync-events`
  reads it; delete `buildMacroEvents` and its `nextWeekday`/`firstFriday`/`addDays`
  helpers; one-off `delete from events where source='macro-seed'`; strip the generator
  from `src/data/macro.ts`.
- `snaptrade-sync`: `seenTickers` diff+delete scoped to `source='snaptrade'`, skipped
  on zero positions or any per-account error. Remove the "Known v1 gap" comment.
- `portfolioRepository.ts` — read `sync_runs` per provider (`schema.sql:108`); surface
  Positions / Prices / Events.
- Deploy by diffing production first, per PR 2's new rule.

### PR 5 · Mutation safety proper

Per-row writes under the authority model. Split `mappers.ts:65 holdingToUpdate` so the
client mapper drops `last_price` / `day_change_*` / `created_at` — it sends all three
today. Then the CSV Merge/Replace picker with preview counts, and per-row delete
confirmation.

### PR 6 · Score recalibration

`holdingMarketValue` flags cost-basis fallbacks as estimated; `positionWeightPct`
routes overrides through `portfolioTotalValue()`; `weightNorm` gets the
portfolio-relative anchor. High/Med/Low tiers ship in the same PR as the anchor, not
after. `EventCard`: weight becomes the hero, score demotes to a badge, delete the bar.
Tests written alongside.

### PR 7 · Morning read, sliced

Scored events into `MonthCalendar` with dot size from weight → Portfolio search /
sort / source filter → mobile compact cards → `formatEventDay` on the agenda. The rest
is optional.

**Dependencies:** PR 6 needs PR 4 (clean weights). PR 5 needs PR 3 (characterization
test). PR 7 depends on nothing and can slip indefinitely.

## Verification

- **P0.1** — import a CSV over the live book; all 40 SnapTrade rows survive.
- **P0.2** — sign in → sign out → no real portfolio visible, `localStorage` cleared.
- **P1.1** — zero `source='macro-seed'` rows; every macro event traceable to a published date.
- **P1.2** — sell a position at Fidelity → sync → row disappears. Simulate a zero-position
  response → nothing is deleted.
- **P3.2** — re-run the distribution query; scores spread across tiers and more than one
  holding can reach High.
- **P2** — `npm run build`, `oxlint`, `vitest` green; CI enforces on PRs.
- Supabase `get_advisors` shows no new findings.
